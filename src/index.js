import SectorTaggerApp from './components/SectorTaggerApp';
import './styles/styles.css';

// // Automatically initialize the app based on data attributes
// document.addEventListener('DOMContentLoaded', () => {
//   const elements = document.querySelectorAll('[data-sector-tagger]');
//   elements.forEach(element => {
//     const appId = element.getAttribute('data-sector-tagger');
//     new SectorTaggerApp(element, appId);
//   });
// });

export { SectorTaggerApp };
